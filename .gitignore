# Local .terraform directories
**/.terraform/*

# .tfstate files (DANGEROUS to push)
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
# (Make sure you don't push your actual db password!)
*.tfvars
*.tfvars.json

# Ignore override files as they are usually local exclusions
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore CLI configuration files
.terraformrc
terraform.rc